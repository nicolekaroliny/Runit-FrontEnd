```
Runit-FrontEnd/
│
├── src/
│   ├── app/
│   │   ├── blog/
│   │   │   ├── page.tsx ...................... 📄 Lista de notícias com paginação
│   │   │   └── [id]/
│   │   │       └── page.tsx .................. 📄 Página individual do post
│   │   │
│   │   ├── components/
│   │   │   ├── BlogPostCard.tsx .............. 🧩 Card de um post
│   │   │   ├── Pagination.tsx ............... 🧩 Componente de paginação
│   │   │   └── ... (componentes existentes)
│   │   │
│   │   ├── globals.css ...................... 🎨 Estilos e temas
│   │   └── ... (arquivos existentes)
│   │
│   ├── lib/
│   │   ├── api/
│   │   │   ├── blogservice.ts ............... 🔌 Serviço de chamadas de API
│   │   │   └── ... (serviços existentes)
│   │   │
│   │   ├── mocks/
│   │   │   └── blog-mock.ts ................. 🧪 Dados de exemplo para testes
│   │   │
│   │   └── ... (arquivos existentes)
│   │
│   └── types/
│       ├── blog.types.ts ................... 📋 Tipos TypeScript
│       └── ... (tipos existentes)
│
├── BLOG_IMPLEMENTATION.md .................. 📚 Documentação técnica
├── BLOG_SETUP.md ........................... 📚 Guia de configuração
└── ... (outros arquivos)
```

## 📊 Fluxo de Dados

```
┌─────────────────────────────────────────────────┐
│  Página: /blog                                   │
│  (src/app/blog/page.tsx)                        │
└────────────┬────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────┐
│  BlogService.getPublishedPosts(page, size)      │
│  (src/lib/api/blogservice.ts)                   │
└────────────┬────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────┐
│  API Backend: GET /api/blog/posts               │
│  com paginação (page, size)                     │
└────────────┬────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────┐
│  PaginatedResponse<BlogPost>                    │
│  { content: [], totalPages: N, ... }            │
└────────────┬────────────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────────────┐
│  Componente: BlogPostCard                       │
│  (renderiza cada post)                          │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│  Componente: Pagination                         │
│  (controla navegação entre páginas)             │
└─────────────────────────────────────────────────┘
```

## 🔄 Fluxo Individual de Post

```
┌──────────────────────────────────────┐
│  URL: /blog/[id]                     │
│  Componente: [id]/page.tsx           │
└────────────┬─────────────────────────┘
             │
             ▼
┌──────────────────────────────────────┐
│  BlogService.getPostById(id)         │
│  API: GET /api/blog/posts/{id}       │
└────────────┬─────────────────────────┘
             │
             ▼
┌──────────────────────────────────────┐
│  BlogPost completo com conteúdo      │
│  { id, title, content, imageUrl }    │
└────────────┬─────────────────────────┘
             │
             ▼
┌──────────────────────────────────────┐
│  Renderiza:                          │
│  - Imagem destaque                   │
│  - Título e categoria                │
│  - Conteúdo HTML                     │
│  - Data de publicação                │
│  - Link voltar à lista               │
└──────────────────────────────────────┘
```

## 🎯 Estados da Página

```
┌─────────────────────┐
│   CARREGANDO        │
│  [Spinner Loading]  │
└─────────────────────┘
         │
         ▼
    ┌─────────────────────────────────┐
    │  SIM  │ Sucesso?  │  NÃO         │
    └───┬───────────────────┬──────────┘
        │                   │
        ▼                   ▼
   ┌────────────────┐  ┌────────────────┐
   │  POSTS CARREGADOS   │  ERRO         │
   │  Grid 3 colunas │  │  Mensagem err │
   │  + Paginação    │  │  + Link volta │
   └────────────────┘  └────────────────┘
```

## 🎨 Responsividade

```
MOBILE (< 768px)
┌──────────────────────┐
│  POST 1              │ 1 coluna
│  POST 2              │
│  POST 3              │
└──────────────────────┘

TABLET (768px - 1024px)
┌─────────────┬─────────────┐
│  POST 1     │  POST 2     │ 2 colunas
│  POST 3     │  POST 4     │
└─────────────┴─────────────┘

DESKTOP (> 1024px)
┌──────────┬──────────┬──────────┐
│ POST 1   │ POST 2   │ POST 3   │ 3 colunas
│ POST 4   │ POST 5   │ POST 6   │
└──────────┴──────────┴──────────┘
```

## 📝 Checklist de Qualidade

✅ TypeScript com tipagem completa
✅ Componentes reutilizáveis
✅ Responsividade mobile-first
✅ Tratamento de erros
✅ Estado de carregamento
✅ Acessibilidade (ARIA labels)
✅ Otimização de performance (lazy loading de imagens)
✅ Paginação eficiente
✅ Estilos consistentes com o tema
✅ Temas claro/escuro suportados

## 🚀 Performance

- **Lazy Loading**: Imagens carregam sob demanda
- **Paginação**: Apenas 12 posts por página
- **Memoização**: BlogPostCard é otimizado
- **Smooth Scroll**: Transições suaves entre páginas
- **Error Boundaries**: Tratamento de erros robusto

## 🔐 Segurança

- Validação de tipos com TypeScript
- Sanitização de URLs de imagens
- CORS habilitado no backend
- Sem injeção de SQL (usando API REST)
- XSS prevention (React escapa HTML)
